<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="cn.com.sure.keypair.dao.KeyPairArchiveDAO">  

<select id="selectAll" parameterType="cn.com.sure.keypair.entry.KeyPairArchive" resultType="cn.com.sure.algorthm.entry.KeyPairAlgorithm">
	select
		id,
		pub_key as pubKey,
		pri_key as priKey,
		key_pair_algorithm as keyPairAlgorithm,
		kpg_task as KpgTask,
		gen_time as genTime,
		start_time as startTime,
		end_time as endTime,
		cert_sn as certSn,
		cert_dn as certDn,
		atch_time as archTime
	from
		keypair_archive
</select>
	
</mapper>